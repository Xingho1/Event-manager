# import sqlConnection as scon
import pyscript
from pyscript import document 
import firebase_admin as fire 
import testfile as tf

def addevent():
    # tf.create_event(event_name = document.getElementById('ev-name-box').text)
    console.log( document.getElementById('ev-name-box').text)
